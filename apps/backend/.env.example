DATABASE_URL="postgresql://postgres:postgres@localhost:5432/formdynamic?schema=public"
PORT=3001
JWT_SECRET="change-me-in-production"

# Sin RESEND_API_KEY, MailerService cae a loguear el contenido del mail en consola (no manda nada real)
RESEND_API_KEY=""
RESEND_FROM="onboarding@resend.dev"
# Opcional — copia oculta (BCC) de cada mail enviado, a una casilla propia de auditoría
RESEND_BCC=""
